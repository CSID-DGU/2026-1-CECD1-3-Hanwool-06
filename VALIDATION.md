# 로컬 검증 기록 · 2026-09-23

## 자동 검사

- `.venv/bin/python -m unittest discover -s tests -q`: 87개 통과
- `npm test`: 13개 통과
- `npm run build`: 통과
- `git diff --check`: 통과

권한·세션·CSRF, 사업소별 문서 접근, 계량기 삭제·복원, 동일 고객번호 중복 차단,
일일 관측 없는 계량기의 위험도 제외, 일일 수집 예약, 설정·인증·빈 응답 구분,
부분 실패 보존, 고객번호 응답 대조, 중복 실행 방지, 스냅샷 게시·정리,
시간 순서에 따른 모델 입력과 잔차 기준, PDF·Excel을 검사했습니다.
외부 응답은 고정 모의 자료를 사용했습니다.

## 실제 브라우저 검사

FastAPI를 `127.0.0.1:8765`에 실행하고 별도 임시 DB에서 확인했습니다.
화면을 데스크톱과 좁은 폭으로 확인했으며 외부 수집·메일·OpenAI 계정은 끈 상태였습니다.

- 로그인, 사업소·검색·호선 필터, 지도 이동·확대·계량기 선택
- 같은 역·같은 호선의 복수 고객번호 전환
- 일일 자료가 있는 계량기의 일일·위험도 화면, 청구 전용의 청구·기간별 비교 화면
- 계량기 추가, 삭제 보관, 복원, 복원 후 자동 연결 확인 예약
- 개별 정보 업데이트의 진행·설정 누락 처리, 자동 수집 시간 저장, 실패 이력
- 청구 PDF 다운로드 이벤트, 통계 조건과 Excel 내보내기 경로
- 당시 관제 집계: 등록 368개, 일일 사용량 80개, 청구 전용 288개 (아래 원본 기준 수정 전)
- 최종 브라우저 콘솔 오류·경고 없음

검증 중 발견한 검색별 건수 불일치, 같은 이름의 계량기 구분, 청구 전용의
불필요한 일일 화면, 완료 후 남아 있던 예약 안내, 큰 수집 패널, 0톤 차트 축,
일부 브라우저의 Blob 다운로드 호환성 문제를 수정했습니다.
다운로드는 세션 확인 후 브라우저 기본 파일 처리로 바꾸고 실제 다운로드를 재확인했습니다.
임시로 등록한 역과 계량기는 검증 데이터 및 검증용 백업에서 제거했습니다.

## 실제 아리수 연결 검증 · 2026-09-23

사용자가 설정한 `.env` 계정으로 실제 로그인·검침 JSON·청구 HTML과 로컬 앱의
정보 업데이트를 검증했습니다. 계정 값·쿠키·CSRF 토큰은 기록하지 않았습니다.

- 전체 실패 원인: 개편 전 `/cs/cyber` 로그인 주소와 `userId/userPwd` 전송으로 HTTP 403 발생.
  현재 `/cyber` 로그인 폼의 CSRF와 `mbrId/pwd`를 사용하고 로그인 뒤 토큰을 갱신했습니다.
- 확정된 과거 검침과 미결산 최신 검침은 별도 JSON 응답으로 제공됩니다.
  두 응답을 고객번호로 검증·병합하며, 당일 미완성 자료는 제외합니다.
- 서로 다른 납기에 반복된 동일 관측은 한 번만 저장합니다. 충돌하는 값은 임의 선택하지 않습니다.
- 청구 목록의 페이지 이동, 고객번호·납기 검증과 잘못된 캐시의 재조회도 확인했습니다.
- 수시분의 고지번호를 고객번호와 분리해 동일 납기의 복수 고지서를 모두 저장합니다.
  기존 DB 청구 47,142행의 ID·내용 보존과 외래키 무결성을 마이그레이션 직후 확인했습니다.
  실제 수시 고지서 2건의 화면 구분과 선택한 고지서의 PDF 다운로드를 확인했습니다.
- 현재 계정의 일일 검침 조회 대상은 77개입니다. 이 제한을 청구서에도 적용했던 오류를 수정했습니다.
  청구서는 비로그인 요금 전체조회의 고객번호+고지서상 성명 경로로 수집합니다.
  회원 고객번호 등록을 자동 변경하지 않았습니다.
- 임시 계량기를 만들지 않고 기존 실제 계약으로 검증했습니다.

이전 전체 작업 7번의 291개 권한 필요 결과는 잘못된 청구서 사전 차단이므로 현재 동작 기준이 아닙니다.
당시 77개 일일 조회 중 73개는 9월 22일까지 정상 관측을 확인했습니다.
문정역8(9/14), 경찰병원역3·잠실새내역2(7/7), 신당역2(6/9)는 아리수가 제공하는
최신 정상 관측이 그 이전입니다. 최근 조회 응답도 재확인했고 장애·미제공 구간을 0으로 만들지 않았습니다.

두 부분 완료 계량기는 수정 후 개별 작업 8·9번으로 다시 검증했습니다.
미아역4의 2026-04-24 음수 사용량, 동대문역사문화공원역4의 2024-05-22 빈 사용량은
해당 관측만 제외하고 같은 달의 유효한 자료를 보존했습니다. 2024-10-18의 3,186톤은
임의 상한으로 버리지 않고 원자료대로 저장됨을 확인했습니다. 제외 일자는 수집 내역에 표시합니다.
이 두 건의 부분 완료는 로그인·수집 연결 실패가 아니라 아리수 과거 원자료의 누락·오류입니다.

실제 브라우저에서 9/22 일일 사용량·2026년 9월 청구·계량기별 연결 상태,
청구 전용 계량기의 기존 청구 보존을 확인했습니다. 공개 조회 검증 결과는 아래에 추가합니다.

## 청구 전용 수집 수정 · 원본 Excel 기준

제공된 3차검토(260518) 원본의 D열 고객번호, F열 고지서상 성명, J열 유무(O)를 대조했습니다.
I열 용도 표기는 시민용/직원용입니다. 현재 등록 368개는 일일+청구 81개와 청구 전용 287개입니다.
기존 80개와 차이는 서울역4(035469767) 한 개이며, 원본에는 O로 기재되어 첫 수집 대기로 표시합니다.

- 일일 조회의 회원 고객번호 등록 조건을 청구서에도 적용했던 사전 차단을 제거했습니다.
- 아리수 공개 전체보기의 고객번호+성명 조회와 정기/수시 상세를 기존 수집·저장 경로에 연결했습니다.
- 왕십리·광화문·동대문 최신 청구를 실제 비로그인 세션으로 확인했습니다.
- 왕십리 2026-09 청구는 2,104,320원, 지하수 4,384톤입니다. 상·하수도 사용량 미표시와 원문 총사용량 0을 구분합니다.
  로컬 웹 업데이트 작업 11번은 일일 조회 없이 청구 8건을 저장했고, 과거 누락 청구도 보충했습니다.
- 실제 웹 화면과 인증된 PDF 응답(1페이지)의 금액·지하수량·출처를 대조했습니다.
- 경복궁 2022-04 정기분 1건과 수시분 2건의 서로 다른 고지번호 및 상세 수집을 검증했습니다.
- 기존 상세가 있는 과거 청구서는 반복 상세 조회를 생략하며 최근 3개월 상세는 정정을 반영합니다.
  2011년의 누락 구간 때문에 이미 저장된 15년치를 재조회하던 작업을 누락 구간별 조회로 수정했습니다.
- 을지로4가역2(000342953)의 원본 성명에 붙은 `(-수전고지제외)`를 제거한 `을지로4가역`이 공식 성명 확인 Y와 동일 고객번호의 최신 청구 조회에 성공함을 확인했습니다. 해당 검증값을 등록부와 초기 seed에 반영했습니다.
- 신규 입력에 고지서상 성명과 청구서만/일일+청구 선택을 추가했습니다. 재시작 후 설정 보존을 확인했습니다.
- 기존 청구·사용량·사용자 정보는 보존했습니다. DB 백업은 `data/runtime/backups/before-public-billing-20260923.sqlite3`입니다.

## 전체 실수집 및 최종 정리

- 운영 DB에서 전체 수집 작업 12번과 개별 재시도 13~15번을 완료했습니다.
  계량기별 마지막 결과는 성공 362개, 일부 완료 6개, 실패 0개입니다.
  368개 모두 청구 조회 및 연결 확인에 성공했습니다.
- 일부 완료 4개: 천왕역7(031842931), 보문역6(032909986), 서울역4(035469767), 역삼역2(035487398).
  일일 대상이지만 현재 아리수 계정에 미등록입니다. 청구서는 정상 저장됐고 기존 일일 자료는 보존됐습니다.
- 나머지 2개: 미아역4의 2026-04-24 음수 사용량, 동대문역사문화공원역4의 2024-05-22 빈 사용량입니다.
  해당 날짜만 제외하며 유효한 일일 자료와 청구서는 저장했습니다.
- 청구 DB는 50,353행, 368개 계량기, 최신 청구월 2026-09입니다.
  공개 청구 수집 직전 백업의 47,716개 청구 ID가 모두 남아 있고 2,637개가 추가됐습니다.
  외래키 오류 0건, 임의 검증 계량기 0개를 확인했습니다.
- 자동 수집은 매일 08:00(한국시간)로 활성화되어 있습니다. 서버 실행 중 동작합니다.
- 미사용 수집·가공 스크립트, 과거 원본과 분석 결과, 사용하지 않는 프론트 스타일·함수·로고를
  `archive/` 아래 원래 경로 또는 출처 주석과 함께 보관했습니다. 운영 코드의 보관 경로 참조는 없습니다.
- 위험도 초기 자료는 `data/app_seed/risk/test_anomalies.csv`로 옮겨 운영 경로를 명시했습니다.
  기존 자료와 바이트가 일치하며, 임시 DB 신규 초기화에서도 368개 계량기와 위험도 11,285행을 확인했습니다.
  운영 사용자·DB·수집 원본·백업과 현재 모델 코드는 유지했습니다.
- 정리 후 서버를 8765 포트에서 재시작했습니다. 실제 브라우저의 대시보드·청구 전용 상세,
  인증된 PDF(1페이지)와 Excel 응답, 지하수와 상수도 통계 분리를 확인했습니다.
  브라우저 오류·경고는 없었습니다. 추가 진단용 원본 HTML과 임시 응답은 제거했습니다.

## 전체 코드·폴더 최종 점검 · 2026-09-23

운영 API, 프론트 전체 소스·스타일, 수집 CLI와 공유 파이프라인, 모델 코드, 테스트,
패키지·환경설정·workflow, seed/정제 자료, 보관 코드·설정과 파일 이동 내역을 분담해 읽었습니다.
외부 패키지 내부를 전부 코드 리뷰한 것은 아니며 설치 의존성은 `pip check`로 확인했습니다.
운영 DB는 읽기 전용 무결성 확인만 했고 사용자 계정·실제 `.env`는 변경하지 않았습니다.

### 추가 발견 후 수정한 문제

| 영역 | 문제와 수정 |
|---|---|
| CLI 청구 저장 | 빈 고지번호와 앞자리 0이 NaN·실수로 바뀌어 저장에서 빠지던 문제를 문자열 유지로 수정 |
| 누락 청구 보충 | 월별 계량기의 한 달 공백, 수시분에 가려진 정기분 누락을 조회하도록 수정 |
| 최초 CLI 갱신 | 관측만 처음 갱신할 때 초기 위험도 자료를 빈 값으로 덮어쓰지 않도록 보존 |
| 동시 수집 | 독립 CLI 4개 진입점도 웹·refresh와 같은 파일 잠금을 사용해 병합 손실 방지 |
| 통계·관리 목록 | 올해~오늘을 통계 기본 기간으로 사용하고 자료 새로고침 시 통계·사용자·삭제 보관함 재조회 |
| 날짜 표시 | 관제 목록의 최신 사용량 날짜와 위험도 분석 날짜를 별도 표시; 비활성 계량기는 전체 기준일 산출에서 제외 |
| 자료 오류 | 음수 등 오류 관측은 업무 요약에서도 자료 확인으로 분리하고 원인 분석·경고메일 요청 차단 |
| 청구 결측 | 상세의 미표시 사용량을 0으로 바꾸지 않도록 화면·통계·PDF·Excel 판정 일치; 상세→요약 재수집에서도 상세 표식 보존 |
| 모델 입력 | 큰 정상 관측을 무조건 자료 오류로 만들지 않으며, 청구 합계의 결측과 같은 달 중복 고지를 구분 |
| AI 응답 | 선택 문자열·행사 필드가 객체 등 잘못된 타입이면 화면으로 넘기지 않고 안전한 실패 안내 반환 |
| 실행 설정 | 빈 선택 데이터 경로는 기본값 사용; 설정 예시의 고정 관리자 비밀번호·무시되는 수신자 항목 제거; self-hosted checkout의 무조건 청소 방지 |
| 보관·의존성 | 과거 일괄 변환 CLI 전체와 누락된 옛 실행 설정을 archive에 보존; 운영에는 실제 청구 집계 함수 유지; 미사용 seaborn·statsmodels 설치 항목 제거 |

### 최종 검증 결과

- Python 87개 + 프론트 13개, 총 100개 테스트 통과; Vite 빌드·diff 검사 통과.
- 설치 의존성 충돌 없음(`pip check`); 운영 DB quick_check=ok, 외래키 오류 0건.
- 기존 Git 파일 이동 대상 342개가 모두 보관되어 있고, 공개 폴더에서 옮긴 청구·역 JSON은 seed와 내용이 일치합니다.
- CSV 13개를 읽어 열 구조를 확인했고, 학습 자료 README의 오래된 행 수·기간을 실파일 기준으로 수정했습니다.
- 운영 자료를 임시 폴더에서 모델 입력으로 재구성: 정상 관측 79,195행, 고객 80개, 최신 2026-09-22, 중복 0행.
  3,186톤 관측을 보존했습니다. 등록된 일일 대상 81개 중 현 학습 이력 기준 충족 63개, 미충족 18개입니다.
- CPU 모델은 기존 실자료와 5개 트리의 작은 설정으로 학습·예측·4종 결과 파일 저장까지 확인했습니다.
  임시 출력만 사용했고 운영 위험도를 바꾸지 않았습니다. 이 검사는 모델 정확도 검증이 아닙니다.
- 수정 후 실제 서버 재시작. 브라우저에서 사용량 9/22·분석 6/13 분리, 기본 통계의 9월 포함,
  왕십리 청구 전용 통계의 사용량 결측·청구금액, API PDF·Excel의 동일 결과를 확인했습니다.
  브라우저 오류·경고 없음, 익명 데이터 API는 401로 차단됩니다.

외부 일일 조회 권한 4개와 아리수 원자료 누락·오류 2개는 위 실수집 기록과 같습니다.
이번 점검에서는 전체 아리수 재수집·실제 메일 발송·유료 AI 호출을 반복하지 않았습니다.
위험도 재학습의 운영 반영 및 OpenAI/SMTP 실연결은 아래 별도 확인 범위를 유지합니다.

## 별도로 남은 확인

승하차 수집에는 `SEOUL_PSGR_KEY`가 필요하며 모델 재분석은 별도 관리 명령으로 실행합니다.
OpenAI 분석·SMTP 발송은 각 설정 후 별도 연결 검증이 필요합니다.
원본 고지서 PDF는 미연동이며 현재 PDF는 저장된 자료로 생성한 조회내역입니다.
사용량은 실제 수집된 마지막 날짜를 표시하며 승하차 2026-04-30, 위험도 2026-06-13과 구분합니다.
전체 기간 재학습의 성능 평가는 별도로 필요하며 CPU 동작 확인을 정확도 검증으로 간주하지 않습니다.
