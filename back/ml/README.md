현재 사용하는 모델 학습·평가 코드입니다. `lightgbm/`에서 CPU 학습, 시간 순 검증과 이상 탐지를 수행합니다.

운영 갱신은 `python -m back.pipelines.refresh --model`로 실행합니다. 이전 분석 실험과 저장된 결과는 [`archive/`](../../archive/README.md)에 보관했으며, 새 결과는 비공개 운영 자료 폴더에 저장합니다.
